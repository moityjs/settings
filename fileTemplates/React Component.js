import React, { PropTypes, Component } from 'react';

export default class ${NAME} extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  state = {
  };

  render() {
    return <div/>; 
  }
}
